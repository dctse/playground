extends layout

block content

    form(name="/sessions", method="post")
        div.input
            span.label Username
            input(type="text", name="user")
        div.input
            span.label Password
            input(type='password', name='password')
        div.actions
            input(type="submit", value="Submit")
